<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!-- Zoho calendar-query REPORT response — ROOT CAUSE of Issue #61.
     Captured pattern from calendar.zoho.com (2026-02-12).

     THIS IS THE KEY FIXTURE: Zoho ignores <c:calendar-data/> in calendar-query.
     The request asked for both getetag and calendar-data, but the response
     only contains getetag. No calendar-data element is present.

     This causes KashCal's CalDavXmlParser.extractICalData() to silently drop
     all responses (currentIcalData is null), resulting in 0 events synced.

     The fix is to use two-step fetch: etags via calendar-query, then data
     via calendar-multiget (see 06_calendar_multiget.xml).

     Zoho quirks in this response:
     - First response is the collection itself (href = .../events/) with empty prop
     - Event hrefs use URL-encoded @ (%40) in UID-based filenames
     - Etag values are numeric timestamps (not quoted strings)
-->
<D:multistatus xmlns:D="DAV:">
  <!-- Collection self-response (no properties) -->
  <D:response>
    <D:href>/caldav/a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4/events/</D:href>
    <D:propstat>
      <D:prop/>
      <D:status>HTTP/1.1 200 OK</D:status>
    </D:propstat>
  </D:response>
  <!-- Event 1: has etag but NO calendar-data -->
  <D:response>
    <D:href>/caldav/a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4/events/aabb0011223344556677889900aabb01%40zoho.com.ics</D:href>
    <D:propstat>
      <D:prop>
        <D:getetag>1770859402675</D:getetag>
      </D:prop>
      <D:status>HTTP/1.1 200 OK</D:status>
    </D:propstat>
  </D:response>
  <!-- Event 2: has etag but NO calendar-data -->
  <D:response>
    <D:href>/caldav/a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4/events/ccdd0011223344556677889900ccdd02%40zoho.com.ics</D:href>
    <D:propstat>
      <D:prop>
        <D:getetag>1770859408075</D:getetag>
      </D:prop>
      <D:status>HTTP/1.1 200 OK</D:status>
    </D:propstat>
  </D:response>
</D:multistatus>
