<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for KashCal.

    Cleartext (HTTP) traffic is allowed because:
    - Self-hosted CalDAV servers (Baikal, Radicale) commonly run on local HTTP
    - URL normalization defaults to HTTPS when no scheme is provided (secure by default)
    - Users must explicitly type http:// to use cleartext (conscious choice)
    - KashCal only connects to user-configured CalDAV servers (no third-party SDKs)

    Android lint may flag this as InsecureBaseConfiguration — this is intentional
    for the self-hosted CalDAV use case.
-->
<network-security-config xmlns:tools="http://schemas.android.com/tools">
    <base-config cleartextTrafficPermitted="true"
        tools:ignore="InsecureBaseConfiguration">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
        Debug builds: Allow user-installed certificates for local testing.
        This does NOT apply to release builds.
    -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
