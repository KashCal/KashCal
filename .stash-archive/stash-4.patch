diff --git a/app/src/main/kotlin/org/onekash/kashcal/SettingsActivity.kt b/app/src/main/kotlin/org/onekash/kashcal/SettingsActivity.kt
index adcc4203..149166af 100644
--- a/app/src/main/kotlin/org/onekash/kashcal/SettingsActivity.kt
+++ b/app/src/main/kotlin/org/onekash/kashcal/SettingsActivity.kt
@@ -103,6 +103,7 @@ class SettingsActivity : ComponentActivity() {
                 val timeFormat by viewModel.timeFormat.collectAsStateWithLifecycle()
                 val firstDayOfWeek by viewModel.firstDayOfWeek.collectAsStateWithLifecycle()
                 val showWeekNumbers by viewModel.showWeekNumbers.collectAsStateWithLifecycle()
+                val quickAddEnabled by viewModel.quickAddEnabled.collectAsStateWithLifecycle()
                 val widgetMaxEventsPerDay by viewModel.widgetMaxEventsPerDay.collectAsStateWithLifecycle()
                 val syncLookbackDays by viewModel.syncLookbackDays.collectAsStateWithLifecycle()
 
@@ -468,6 +469,8 @@ class SettingsActivity : ComponentActivity() {
                             onFirstDayOfWeekChange = viewModel::setFirstDayOfWeek,
                             showWeekNumbers = showWeekNumbers,
                             onShowWeekNumbersChange = viewModel::setShowWeekNumbers,
+                            quickAddEnabled = quickAddEnabled,
+                            onQuickAddEnabledChange = viewModel::setQuickAddEnabled,
                             widgetMaxEventsPerDay = widgetMaxEventsPerDay,
                             onWidgetMaxEventsPerDayChange = viewModel::setWidgetMaxEventsPerDay,
                             // Version footer
diff --git a/app/src/main/kotlin/org/onekash/kashcal/data/preferences/UserPreferencesRepository.kt b/app/src/main/kotlin/org/onekash/kashcal/data/preferences/UserPreferencesRepository.kt
index 9e6099e1..7e74bdff 100644
--- a/app/src/main/kotlin/org/onekash/kashcal/data/preferences/UserPreferencesRepository.kt
+++ b/app/src/main/kotlin/org/onekash/kashcal/data/preferences/UserPreferencesRepository.kt
@@ -168,6 +168,17 @@ class UserPreferencesRepository @Inject constructor(
         dataStore.setDefaultEventDuration(minutes)
     }
 
+    /**
+     * Quick Add enabled state (default true).
+     * When enabled, FAB opens quick add overlay instead of full event form.
+     */
+    val quickAddEnabled: Flow<Boolean>
+        get() = dataStore.quickAddEnabled
+
+    suspend fun setQuickAddEnabled(enabled: Boolean) {
+        dataStore.setQuickAddEnabled(enabled)
+    }
+
     // ========== Onboarding ==========
 
     /**
diff --git a/app/src/main/kotlin/org/onekash/kashcal/ui/screens/AccountSettingsScreen.kt b/app/src/main/kotlin/org/onekash/kashcal/ui/screens/AccountSettingsScreen.kt
index 06622009..ec87d891 100644
--- a/app/src/main/kotlin/org/onekash/kashcal/ui/screens/AccountSettingsScreen.kt
+++ b/app/src/main/kotlin/org/onekash/kashcal/ui/screens/AccountSettingsScreen.kt
@@ -10,6 +10,7 @@ import androidx.compose.foundation.verticalScroll
 import androidx.compose.material.icons.Icons
 import androidx.compose.material.icons.automirrored.filled.ArrowBack
 import androidx.compose.material.icons.filled.DateRange
+import androidx.compose.material.icons.filled.EditNote
 import androidx.compose.material.icons.filled.FileDownload
 import androidx.compose.material.icons.filled.FileUpload
 import androidx.compose.material.icons.filled.Check
@@ -223,6 +224,8 @@ fun AccountSettingsScreen(
     onFirstDayOfWeekChange: (Int) -> Unit = {},
     showWeekNumbers: Boolean = false,
     onShowWeekNumbersChange: (Boolean) -> Unit = {},
+    quickAddEnabled: Boolean = true,
+    onQuickAddEnabledChange: (Boolean) -> Unit = {},
     widgetMaxEventsPerDay: Int = 5,
     onWidgetMaxEventsPerDayChange: (Int) -> Unit = {},
     // Version footer (Checkpoint 9)
@@ -309,6 +312,8 @@ fun AccountSettingsScreen(
                     onFirstDayOfWeekChange = onFirstDayOfWeekChange,
                     showWeekNumbers = showWeekNumbers,
                     onShowWeekNumbersChange = onShowWeekNumbersChange,
+                    quickAddEnabled = quickAddEnabled,
+                    onQuickAddEnabledChange = onQuickAddEnabledChange,
                     widgetMaxEventsPerDay = widgetMaxEventsPerDay,
                     onWidgetMaxEventsPerDayChange = onWidgetMaxEventsPerDayChange,
                     showAddSubscriptionDialogFromIntent = uiState.showAddSubscriptionDialog,
@@ -439,6 +444,8 @@ private fun FlatSettingsContent(
     onFirstDayOfWeekChange: (Int) -> Unit,
     showWeekNumbers: Boolean,
     onShowWeekNumbersChange: (Boolean) -> Unit,
+    quickAddEnabled: Boolean,
+    onQuickAddEnabledChange: (Boolean) -> Unit,
     widgetMaxEventsPerDay: Int,
     onWidgetMaxEventsPerDayChange: (Int) -> Unit,
     showAddSubscriptionDialogFromIntent: Boolean,
@@ -637,6 +644,12 @@ private fun FlatSettingsContent(
                 subtitle = formatDuration(defaultEventDuration),
                 onClick = { showEventDurationSheet = true }
             )
+            SettingsRow(
+                icon = Icons.Default.EditNote,
+                label = "Quick Add",
+                subtitle = if (quickAddEnabled) "On" else "Off",
+                onClick = { onQuickAddEnabledChange(!quickAddEnabled) }
+            )
             SettingsRow(
                 icon = Icons.Default.CalendarMonth,
                 label = "Widget Event Limit",
diff --git a/app/src/main/kotlin/org/onekash/kashcal/ui/viewmodels/AccountSettingsViewModel.kt b/app/src/main/kotlin/org/onekash/kashcal/ui/viewmodels/AccountSettingsViewModel.kt
index eb8dbee9..7afd8461 100644
--- a/app/src/main/kotlin/org/onekash/kashcal/ui/viewmodels/AccountSettingsViewModel.kt
+++ b/app/src/main/kotlin/org/onekash/kashcal/ui/viewmodels/AccountSettingsViewModel.kt
@@ -274,6 +274,9 @@ class AccountSettingsViewModel @Inject constructor(
     private val _showWeekNumbers = MutableStateFlow(false)
     val showWeekNumbers: StateFlow<Boolean> = _showWeekNumbers.asStateFlow()
 
+    private val _quickAddEnabled = MutableStateFlow(true)
+    val quickAddEnabled: StateFlow<Boolean> = _quickAddEnabled.asStateFlow()
+
     private val _widgetMaxEventsPerDay = MutableStateFlow(5)
     val widgetMaxEventsPerDay: StateFlow<Int> = _widgetMaxEventsPerDay.asStateFlow()
 
@@ -591,6 +594,11 @@ class AccountSettingsViewModel @Inject constructor(
                 _showWeekNumbers.value = show
             }
         }
+        viewModelScope.launch {
+            dataStore.quickAddEnabled.collect { enabled ->
+                _quickAddEnabled.value = enabled
+            }
+        }
         viewModelScope.launch {
             dataStore.widgetMaxEventsPerDay.collect { count ->
                 _widgetMaxEventsPerDay.value = count
@@ -641,6 +649,15 @@ class AccountSettingsViewModel @Inject constructor(
         }
     }
 
+    /**
+     * Update the quick add enabled preference.
+     */
+    fun setQuickAddEnabled(enabled: Boolean) {
+        viewModelScope.launch {
+            dataStore.setQuickAddEnabled(enabled)
+        }
+    }
+
     /**
      * Update the widget max events per day preference.
      * Also triggers widget refresh since widgets display events.
diff --git a/app/src/test/kotlin/org/onekash/kashcal/ui/viewmodels/HomeViewModelTest.kt b/app/src/test/kotlin/org/onekash/kashcal/ui/viewmodels/HomeViewModelTest.kt
index 82bf3066..4b737089 100644
--- a/app/src/test/kotlin/org/onekash/kashcal/ui/viewmodels/HomeViewModelTest.kt
+++ b/app/src/test/kotlin/org/onekash/kashcal/ui/viewmodels/HomeViewModelTest.kt
@@ -3324,30 +3324,16 @@ class HomeViewModelTest {
     }
 
     @Test
-    fun `setDefaultViewMode persists to DataStore`() = runTest {
+    fun `setViewMode auto-persists to DataStore`() = runTest {
         val viewModel = createViewModel()
         advanceUntilIdle()
 
-        viewModel.setDefaultViewMode(ViewMode.AGENDA)
+        viewModel.setViewMode(ViewMode.AGENDA)
         advanceUntilIdle()
 
         coVerify { dataStore.setDefaultCalendarView("agenda") }
     }
 
-    @Test
-    fun `showViewPicker and hideViewPicker toggle state`() = runTest {
-        val viewModel = createViewModel()
-        advanceUntilIdle()
-
-        assertFalse(viewModel.uiState.value.showViewPicker)
-
-        viewModel.showViewPicker()
-        assertTrue(viewModel.uiState.value.showViewPicker)
-
-        viewModel.hideViewPicker()
-        assertFalse(viewModel.uiState.value.showViewPicker)
-    }
-
     @Test
     fun `init loads default view from DataStore`() = runTest {
         // Override default view to AGENDA (must be set before createViewModel)
@@ -3361,7 +3347,6 @@ class HomeViewModelTest {
         advanceUntilIdle()
 
         assertEquals(ViewMode.AGENDA, viewModel.uiState.value.viewMode)
-        assertEquals(ViewMode.AGENDA, viewModel.uiState.value.defaultViewMode)
     }
 
     // ==================== Occurrence Extension Tests ====================
