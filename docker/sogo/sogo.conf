{
  WOPort = 0.0.0.0:20000;
  WOWorkersCount = 1;
  WOLogFile = /var/log/sogo/sogo.log;

  SOGoTimeZone = "UTC";
  SOGoMailDomain = "test.local";

  /* Database connections */
  SOGoProfileURL = "postgresql://sogo:sogopass@sogo-db:5432/sogo/sogo_user_profile";
  OCSFolderInfoURL = "postgresql://sogo:sogopass@sogo-db:5432/sogo/sogo_folder_info";
  OCSSessionsFolderURL = "postgresql://sogo:sogopass@sogo-db:5432/sogo/sogo_sessions_folder";
  OCSEMailAlarmsFolderURL = "postgresql://sogo:sogopass@sogo-db:5432/sogo/sogo_alarms_folder";

  /* SQL-based authentication (no LDAP/IMAP needed) */
  SOGoUserSources = (
    {
      type = sql;
      id = users;
      viewURL = "postgresql://sogo:sogopass@sogo-db:5432/sogo/sogo_users";
      canAuthenticate = YES;
      isAddressBook = NO;
      userPasswordAlgorithm = plain;
    }
  );

  /* Enable CalDAV, disable mail */
  SOGoCalendarDAVAccessEnabled = YES;
  SOGoAddressBookDAVAccessEnabled = NO;

  /* Memcached */
  SOGoMemcachedHost = "127.0.0.1";

  /* Disable password change */
  SOGoPasswordChangeEnabled = NO;
}